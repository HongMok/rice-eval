# RICE AI 评估系统 — 项目文档

## 项目概述

RICE AI 是一款面向特需儿童康复机构的智能评估系统，核心功能是将专业评估工具（PEP-3、C-PEP-3、VB-MAPP）的评估数据，通过 AI 自动生成评估报告和个别化教育计划（IEP）。

## 行业背景

- **目标用户**：ABA（应用行为分析）康复机构的康复师、督导、管理员、家长
- **核心评估工具**：
  - **PEP-3**：心理教育量表第三版，6个发展副测验 + 4个行为副测验 + 3个照顾者报告，评分体系 P(通过)/E(萌芽)/F(不通过)
  - **C-PEP-3**：中文修订版，7个发展领域(96题) + 5个病理行为领域(44题) + 补充项目(32题)
  - **VB-MAPP**：语言行为里程碑评估，170个里程碑(16领域×3阶段) + 24项障碍评估 + 18项转衔评估
- **核心流程**：评估答题 → 自动计分 → AI生成报告 → AI生成IEP → 导出文档

## 项目结构

```
config/                        # AI Prompt 配置
  ├── report-prompt-*.md       # 评估报告生成规则（PEP3/CPEP/VB-MAPP）
  ├── iep-prompt-*.md          # IEP生成规则
  ├── tool-intro-*.md          # 评估工具介绍文案
  ├── tool-disclaimer-*.md     # 免责声明
  └── eval-type-config.json    # 评估工具交互配置

data/                          # 评估配置数据
  ├── pep3-config-v5.csv       # PEP-3 题目配置
  ├── cpep-config-v3.csv       # C-PEP 题目配置
  ├── vbmapp-config-v4.csv     # VB-MAPP 题目配置（含里程碑+障碍+转衔）
  └── PEP-3/ VB/ C-PEP/       # 原始量表资料

features/                      # HTML 原型
  ├── assessment-admin/pad/    # 后台管理端（评估工具管理）
  └── assessment-app/          # 康复师App端（答题、结果、列表）
      ├── pad/                 # PAD/Web端
      └── mobile/              # 手机端

design/DESIGN.md               # 设计规范（后台紫色#7C3AED / App蓝色#3B9BF5）
```

## CSV 配置表结构

所有评估工具的题目配置统一为 CSV 格式：
- 表头：排序, 一级分类, 二级分类, 评估项目, 操作描述, 所需材料, 适用年龄, 是否必答, 评分_a_分值, 评分_a_标签, 评分_a_说明, ...评分_f
- 所有字段用双引号包裹（QUOTE_ALL），UTF-8 编码
- 评分说明放在对应分值的评分说明列，不放在操作描述列

## AI 生成规则要点

### 评估报告
- 重点是**结果解读和干预方向**，不是分数罗列
- 以连续专业段落呈现，禁止"1、2、3"列表形式
- 必须基于具体条目表现分析（会什么、不会什么、差距在哪里）
- 每个领域都要包含干预建议，对应未掌握条目
- VB-MAPP 里程碑分析使用三段式：领域定义 → 具体观察描述 → 得分+课程定位
- 障碍分析使用三段式：现象描述 → 功能分析 → 干预方案

### IEP 生成
- 评估数据 → 筛选薄弱领域 → 确定优先级 → 匹配课包干预项目 → 控制数量 → 生成表格
- 领域映射使用动态匹配规则（语义匹配+能力维度匹配），不写死映射表
- 所有项目名称必须与课包完全一致，不能编造

## 飞书集成

- 需求文档维护在飞书云端，使用 `lark-cli` 操作
- 评估报告和IEP的 prompt 规则同步维护在飞书表格中
- HTML 原型部署在 GitHub Pages：`https://hongmok.github.io/rice-eval/features/...`

## 开发规范

- Git 提交：不主动提交，等用户说"提交git"再执行，commit message 用中文
- HTML 原型：模拟数据存 localStorage，key 前缀 `rice_assessment_`，每页有重置按钮
- 数据变更：必须走全链路检查清单（CSV → 后台 → App → 缓存 → 文档）
- 多端：PAD端和手机端同步设计，需求打磨阶段只改 pad/
